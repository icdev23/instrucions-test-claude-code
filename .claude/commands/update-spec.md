Você deve executar a tarefa de **Atualizar Especificação Funcional**.

**Instruções**:
1. Leia o arquivo `instructions/tasks/task_update_spec.md` para entender o fluxo completo da tarefa
2. Leia o arquivo `instructions/context/context_task.md` para entender os tipos de inputs disponíveis
3. Leia o arquivo `instructions/context/context_project.md` para obter contexto do projeto atual
4. Execute a tarefa conforme as instruções específicas do arquivo de tarefa
5. Salve o output em `outputs/specs/` com nome descritivo (ex: `updated_YYYY-MM-DD_nome-funcionalidade.md`)

**Importante**: O utilizador DEVE fornecer a especificação funcional atual que será atualizada.
